# 2026-04-03

- Added durable workspace memory files so the `vpstudio` agent has the same startup scaffolding as the other split workspaces.
- Confirmed this repo is Swift / Xcode based and should not be probed as a Node project.
- Canonical project anchors are `VPStudio.xcodeproj`, `Package.swift`, and `Info.plist`.
