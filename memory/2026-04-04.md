# 2026-04-04

Placeholder daily memory file. Populate with validated notes when today's summary is consolidated.
# 2026-04-04

Placeholder daily memory file. Populate with validated notes when today's summary is consolidated.

## Durable notes

- UI remodel automation is active via cron: `VPStudio UI Remodel — 15m / 3-agent / gpt-5.4` (job id `370443aa-a1db-413a-8809-8e2078292e93`). Orchestrator prompt lives at `automation/ui-remodel/prompts/orchestrator.txt`.
- `docs/UI_REMODEL_PLAN.md` is now the living source of truth for UI remodel work, run log, current priorities, and remaining gaps.
- High-value UI remodel work landed on 2026-04-04:
  - series-detail scroll regression fix + regression coverage in `VPStudioTests/ViewModels/DetailScrollRegressionTests.swift`
  - Discover hierarchy/scanability policy pass in `VPStudio/Views/Windows/Discover/DiscoverView.swift` + tests in `VPStudioTests/ViewModels/DiscoverHierarchyPolicyTests.swift`
  - Library header/folder clarity pass via `LibraryActionRowPolicy` and `LibraryFolderLabelPolicy` + tests in `VPStudioTests/LibraryViewPolicyTests.swift`
  - Discover AI curated redesign to a banner-first module with `aiHeroPreview` + tests in `VPStudioTests/ViewModels/DiscoverAITests.swift`
  - Library/Downloads loading-surface normalization + contracts in `VPStudioTests/LoadingSurfaceContractTests.swift`
  - shared TMDB setup-error consistency across Discover/Search via `AppError.tmdbSetupRequired(feature:)` + tests in `DiscoverViewModelTests`, `SearchViewModelTests`, and `SearchViewModelExplorePhaseTests`
  - refresh-level loading pass: Discover/Search now preserve content during refresh, and Detail preserves same-preview content during metadata reloads; tests in `VPStudioTests/ViewModels/RefreshLoadingPolicyTests.swift`
  - series-detail season-switch scoped-loading pass recorded in `docs/UI_REMODEL_PLAN.md`; still needs runtime visual QA
  - Downloads root-error surface converted from raw string-first rendering to typed `AppError`-based root/inline error surfaces; tests in `VPStudioTests/ViewModels/DownloadsViewModelTests.swift`, `VPStudioTests/DownloadsErrorSurfacePolicyTests.swift`, and `VPStudioTests/ErrorSurfaceContractTests.swift`
- Important operational note: shell execution was blocked in this session (`exec denied: allowlist miss`), so runtime test execution and git commits could not be performed from chat. Future sessions should run tests and commit when shell access is available.
- Current next-step direction from the remodel plan: second error-consistency pass for Library/Settings unless runtime QA reveals a higher-severity regression.
- Known remaining weak spots:
  - series-detail season-switch treatment needs simulator/device visual QA
  - Library still overloads `statusMessage` for progress/success/info/failure and can collapse load failures into a false empty-state path
  - Settings still has silent `try?`/string-first save-load failures on at least some screens and needs a typed error-surface pass
- Working recommendation for Library duplicate folder names: keep them as a real validation error (keep the sheet open) instead of auto-reusing an existing folder. This was recommended but not user-confirmed.
# 2026-04-04

Placeholder daily memory file. Populate with validated notes when today's summary is consolidated.

## Durable notes

- UI remodel automation is active via cron: `VPStudio UI Remodel — 15m / 3-agent / gpt-5.4` (job id `370443aa-a1db-413a-8809-8e2078292e93`). Orchestrator prompt lives at `automation/ui-remodel/prompts/orchestrator.txt`.
- `docs/UI_REMODEL_PLAN.md` is now the living source of truth for UI remodel work, run log, current priorities, and remaining gaps.
- High-value UI remodel work landed on 2026-04-04:
  - series-detail scroll regression fix + regression coverage in `VPStudioTests/ViewModels/DetailScrollRegressionTests.swift`
  - Discover hierarchy/scanability policy pass in `VPStudio/Views/Windows/Discover/DiscoverView.swift` + tests in `VPStudioTests/ViewModels/DiscoverHierarchyPolicyTests.swift`
  - Library header/folder clarity pass via `LibraryActionRowPolicy` and `LibraryFolderLabelPolicy` + tests in `VPStudioTests/LibraryViewPolicyTests.swift`
  - Discover AI curated redesign to a banner-first module with `aiHeroPreview` + tests in `VPStudioTests/ViewModels/DiscoverAITests.swift`
  - Library/Downloads loading-surface normalization + contracts in `VPStudioTests/LoadingSurfaceContractTests.swift`
  - shared TMDB setup-error consistency across Discover/Search via `AppError.tmdbSetupRequired(feature:)` + tests in `DiscoverViewModelTests`, `SearchViewModelTests`, and `SearchViewModelExplorePhaseTests`
  - refresh-level loading pass: Discover/Search now preserve content during refresh, and Detail preserves same-preview content during metadata reloads; tests in `VPStudioTests/ViewModels/RefreshLoadingPolicyTests.swift`
  - series-detail season-switch scoped-loading pass recorded in `docs/UI_REMODEL_PLAN.md`; still needs runtime visual QA
  - Downloads root-error surface converted from raw string-first rendering to typed `AppError`-based root/inline error surfaces; tests in `VPStudioTests/ViewModels/DownloadsViewModelTests.swift`, `VPStudioTests/DownloadsErrorSurfacePolicyTests.swift`, and `VPStudioTests/ErrorSurfaceContractTests.swift`
- Important operational note: shell execution was blocked in this session (`exec denied: allowlist miss`), so runtime test execution and git commits could not be performed from chat. Future sessions should run tests and commit when shell access is available.
- Current next-step direction from the remodel plan: second error-consistency pass for Library/Settings unless runtime QA reveals a higher-severity regression.
- Known remaining weak spots:
  - series-detail season-switch treatment needs simulator/device visual QA
  - Library still overloads `statusMessage` for progress/success/info/failure and can collapse load failures into a false empty-state path
  - Settings still has silent `try?`/string-first save-load failures on at least some screens and needs a typed error-surface pass
- Working recommendation for Library duplicate folder names: keep them as a real validation error (keep the sheet open) instead of auto-reusing an existing folder. This was recommended but not user-confirmed.

## Cron / Model Configuration (2026-04-04 evening)

- **Codex limit hit** (pro plan, ~6226 min cooldown ~4.3 days) due to fallback behavior — user is furious.
- **User directive: REMOVE ALL FALLBACKS from everywhere.** No fallbacks. Jobs must fail visibly at the intended model, not silently spill to another model.
- Ollama local Qwen (`ollama/qwen3.5:4b`) was just added by user.
- Direct smoke test `sessions_spawn` with `ollama/qwen3.5:4b` → **`model not allowed`** (runtime allowlist rejection).
- Fallback config audit subagent failed (same Codex rate limit issue hitting the subagent).
- User has been saying "go ahead and do it" multiple times (20:13, 20:54, 20:57, 21:01) — session was stuck waiting on the smoke test + config audit which both failed.
- **NOTHING was applied.** This session ended without doing the requested work.
- Cron jobs remain in their current state (MiniMax/Gemini/Grok configured per prior session).
- Jobs JSON: `/Users/openclaw/.openclaw/cron/jobs.json` — 34 jobs, model-bearing ones include:
  - `twitter-trending` → Grok
  - `dailies-media-pack` → Gemini Flash Lite
  - `dailies-memes-pack` → Gemini Flash Lite
  - `youtube-tech-daily` → Gemini Flash Lite
  - Many internal/repetitive jobs → MiniMax (intended Qwen batch once model is allowed)
  - `weekly-best-of` → default model
  - `outreach-goodnight` → default model
- **Next session must:**
  1. Re-test `ollama/qwen3.5:4b` directly once, confirm it's now allowed
  2. Audit and remove fallback settings from config level (models config, agents config)
  3. Apply cron model split: first-wave Qwen batch to `ollama/qwen3.5:4b`, rest to MiniMax, Grok/Gemini exceptions stay
  4. Ensure NO fallbacks anywhere — jobs fail at model, don't spill


## Cron / Model Config — UPDATE (2026-04-04 late evening)

- **Config edit applied**: `qwen3.5:4b` was manually added to:
  1. `models.providers.ollama.models[]` — with name "Qwen 3.5 4B", reasoning:true, contextWindow:128000, maxTokens:8192, cost:0
  2. `agents.defaults.models["ollama/qwen3.5:4b"].params` — maxTokens:8192
- Gateway restart is still needed for changes to take effect — `exec` is blocked, could not call `openclaw gateway restart` or `gateway(action="config.patch")` successfully due to hash mismatch issues
- **Pending**: After gateway restart, need to re-test `ollama/qwen3.5:4b` smoke test to confirm model passes runtime allowlist
- **Pending**: After Qwen confirmed working, repoint cron jobs:
  - First wave → `ollama/qwen3.5:4b`: git-maintenance, daily-cost-report, Morning Brief, security:deep-scan, kairos-poll, kairos-morning-digest, kairos-evening-digest, prometheus-learn, prometheus-consolidate, VPStudio bugscan auto-commit
  - Remaining model-bearing jobs → `litellm/MiniMax-M2.7`
  - Grok/Gemini exceptions stay as-is
- `exec` tool is still blocked (allowlist miss) — cannot restart gateway or run shell commands from chat


# 2026-04-04 — late update

## Cron model migration — second batch running

- **Second batch of cron edits now in flight** (MiniMax bulk + qwen model additions + enables), spawned as a subagent. Results pending.
- Previous batch (22 edits via subagent) had silently failed — subagent returned empty output due to rate limits hitting that subagent's own model.
- Verified that MiniMax subagents work fine (tested with verify-jobs subagent — returned 19k tokens output).
- **Direct file writes via read+write tool are the reliable path** — subagents keep getting rate-limited on their default model and silently failing.
- Qwen smoke test is still `model not allowed` — gateway may need a restart to pick up the newly added qwen3.5:4b config.
- All model-bearing cron jobs still show `openai-codex/gpt-5.4` in verify-read — edits not yet confirmed applied.
- `exec` tool remains blocked in this session — cannot restart gateway or run shell.
- Remaining jobs needing attention (MiniMax, no model, or disabled) are in the second batch.


## Cron model migration — completed

Two subagent batches ran today to migrate cron jobs off `openai-codex/gpt-5.4` (rate-limited) onto local/specialty models:
- **Batch 1 (22 edits)**: silently failed due to subagent rate limits on its own default model
- **Batch 2 (13 edits via subagent `c4608a4a`)**: all succeeded — 13/13

**Final model map applied:**
- `ollama/qwen3.5:4b` (local): git-maintenance, daily-cost-report, security:deep-scan, kairos-poll, kairos-morning-digest, kairos-evening-digest, kairos-poll (added model field), prometheus-learn, prometheus-consolidate, Morning Brief (enabled)
- `litellm/MiniMax-M2.7`: project-report, cleta-daily-update, dailies-channels, for-you-brief, outreach-morning, outreach-checkin, finance-brief, trend-content-studio, outreach-goodnight, outreach-goodnight-shania, outreach-morning-shania, outreach-checkin-shania, nightshift-keep-working
- `litellm/openrouter-grok-4.1-fast`: twitter-trending (untouched, intentional)
- `litellm/google-gemini-3.1-flash-lite-preview`: dailies-media-pack, dailies-memes-pack, youtube-tech-daily (untouched, intentional)
- Bugscan pipeline (5 jobs on `codex-spark`) + `VPStudio UI Remodel` were not in scope for migration

**Operational notes:**
- `exec` tool was blocked all session — could not restart gateway to pick up newly added qwen model config; Qwen smoke test still showed "model not allowed" at session end
- Direct file reads/writes via tool are the reliable path; subagents fail silently when their own default model hits rate limits
- `cron` tool unavailable in this session context
- Memory write deferred several times; current session should compact with durable notes now stored
# 2026-04-04

Placeholder daily memory file. Populate with validated notes when today's summary is consolidated.
# 2026-04-04

Placeholder daily memory file. Populate with validated notes when today's summary is consolidated.

## Durable notes

- UI remodel automation is active via cron: `VPStudio UI Remodel — 15m / 3-agent / gpt-5.4` (job id `370443aa-a1db-413a-8809-8e2078292e93`). Orchestrator prompt lives at `automation/ui-remodel/prompts/orchestrator.txt`.
- `docs/UI_REMODEL_PLAN.md` is now the living source of truth for UI remodel work, run log, current priorities, and remaining gaps.
- High-value UI remodel work landed on 2026-04-04:
  - series-detail scroll regression fix + regression coverage in `VPStudioTests/ViewModels/DetailScrollRegressionTests.swift`
  - Discover hierarchy/scanability policy pass in `VPStudio/Views/Windows/Discover/DiscoverView.swift` + tests in `VPStudioTests/ViewModels/DiscoverHierarchyPolicyTests.swift`
  - Library header/folder clarity pass via `LibraryActionRowPolicy` and `LibraryFolderLabelPolicy` + tests in `VPStudioTests/LibraryViewPolicyTests.swift`
  - Discover AI curated redesign to a banner-first module with `aiHeroPreview` + tests in `VPStudioTests/ViewModels/DiscoverAITests.swift`
  - Library/Downloads loading-surface normalization + contracts in `VPStudioTests/LoadingSurfaceContractTests.swift`
  - shared TMDB setup-error consistency across Discover/Search via `AppError.tmdbSetupRequired(feature:)` + tests in `DiscoverViewModelTests`, `SearchViewModelTests`, and `SearchViewModelExplorePhaseTests`
  - refresh-level loading pass: Discover/Search now preserve content during refresh, and Detail preserves same-preview content during metadata reloads; tests in `VPStudioTests/ViewModels/RefreshLoadingPolicyTests.swift`
  - series-detail season-switch scoped-loading pass recorded in `docs/UI_REMODEL_PLAN.md`; still needs runtime visual QA
  - Downloads root-error surface converted from raw string-first rendering to typed `AppError`-based root/inline error surfaces; tests in `VPStudioTests/ViewModels/DownloadsViewModelTests.swift`, `VPStudioTests/DownloadsErrorSurfacePolicyTests.swift`, and `VPStudioTests/ErrorSurfaceContractTests.swift`
- Important operational note: shell execution was blocked in this session (`exec denied: allowlist miss`), so runtime test execution and git commits could not be performed from chat. Future sessions should run tests and commit when shell access is available.
- Current next-step direction from the remodel plan: second error-consistency pass for Library/Settings unless runtime QA reveals a higher-severity regression.
- Known remaining weak spots:
  - series-detail season-switch treatment needs simulator/device visual QA
  - Library still overloads `statusMessage` for progress/success/info/failure and can collapse load failures into a false empty-state path
  - Settings still has silent `try?`/string-first save-load failures on at least some screens and needs a typed error-surface pass
- Working recommendation for Library duplicate folder names: keep them as a real validation error (keep the sheet open) instead of auto-reusing an existing folder. This was recommended but not user-confirmed.
# 2026-04-04

Placeholder daily memory file. Populate with validated notes when today's summary is consolidated.

## Durable notes

- UI remodel automation is active via cron: `VPStudio UI Remodel — 15m / 3-agent / gpt-5.4` (job id `370443aa-a1db-413a-8809-8e2078292e93`). Orchestrator prompt lives at `automation/ui-remodel/prompts/orchestrator.txt`.
- `docs/UI_REMODEL_PLAN.md` is now the living source of truth for UI remodel work, run log, current priorities, and remaining gaps.
- High-value UI remodel work landed on 2026-04-04:
  - series-detail scroll regression fix + regression coverage in `VPStudioTests/ViewModels/DetailScrollRegressionTests.swift`
  - Discover hierarchy/scanability policy pass in `VPStudio/Views/Windows/Discover/DiscoverView.swift` + tests in `VPStudioTests/ViewModels/DiscoverHierarchyPolicyTests.swift`
  - Library header/folder clarity pass via `LibraryActionRowPolicy` and `LibraryFolderLabelPolicy` + tests in `VPStudioTests/LibraryViewPolicyTests.swift`
  - Discover AI curated redesign to a banner-first module with `aiHeroPreview` + tests in `VPStudioTests/ViewModels/DiscoverAITests.swift`
  - Library/Downloads loading-surface normalization + contracts in `VPStudioTests/LoadingSurfaceContractTests.swift`
  - shared TMDB setup-error consistency across Discover/Search via `AppError.tmdbSetupRequired(feature:)` + tests in `DiscoverViewModelTests`, `SearchViewModelTests`, and `SearchViewModelExplorePhaseTests`
  - refresh-level loading pass: Discover/Search now preserve content during refresh, and Detail preserves same-preview content during metadata reloads; tests in `VPStudioTests/ViewModels/RefreshLoadingPolicyTests.swift`
  - series-detail season-switch scoped-loading pass recorded in `docs/UI_REMODEL_PLAN.md`; still needs runtime visual QA
  - Downloads root-error surface converted from raw string-first rendering to typed `AppError`-based root/inline error surfaces; tests in `VPStudioTests/ViewModels/DownloadsViewModelTests.swift`, `VPStudioTests/DownloadsErrorSurfacePolicyTests.swift`, and `VPStudioTests/ErrorSurfaceContractTests.swift`
- Important operational note: shell execution was blocked in this session (`exec denied: allowlist miss`), so runtime test execution and git commits could not be performed from chat. Future sessions should run tests and commit when shell access is available.
- Current next-step direction from the remodel plan: second error-consistency pass for Library/Settings unless runtime QA reveals a higher-severity regression.
- Known remaining weak spots:
  - series-detail season-switch treatment needs simulator/device visual QA
  - Library still overloads `statusMessage` for progress/success/info/failure and can collapse load failures into a false empty-state path
  - Settings still has silent `try?`/string-first save-load failures on at least some screens and needs a typed error-surface pass
- Working recommendation for Library duplicate folder names: keep them as a real validation error (keep the sheet open) instead of auto-reusing an existing folder. This was recommended but not user-confirmed.

## Cron / Model Configuration (2026-04-04 evening)

- **Codex limit hit** (pro plan, ~6226 min cooldown ~4.3 days) due to fallback behavior — user is furious.
- **User directive: REMOVE ALL FALLBACKS from everywhere.** No fallbacks. Jobs must fail visibly at the intended model, not silently spill to another model.
- Ollama local Qwen (`ollama/qwen3.5:4b`) was just added by user.
- Direct smoke test `sessions_spawn` with `ollama/qwen3.5:4b` → **`model not allowed`** (runtime allowlist rejection).
- Fallback config audit subagent failed (same Codex rate limit issue hitting the subagent).
- User has been saying "go ahead and do it" multiple times (20:13, 20:54, 20:57, 21:01) — session was stuck waiting on the smoke test + config audit which both failed.
- **NOTHING was applied.** This session ended without doing the requested work.
- Cron jobs remain in their current state (MiniMax/Gemini/Grok configured per prior session).
- Jobs JSON: `/Users/openclaw/.openclaw/cron/jobs.json` — 34 jobs, model-bearing ones include:
  - `twitter-trending` → Grok
  - `dailies-media-pack` → Gemini Flash Lite
  - `dailies-memes-pack` → Gemini Flash Lite
  - `youtube-tech-daily` → Gemini Flash Lite
  - Many internal/repetitive jobs → MiniMax (intended Qwen batch once model is allowed)
  - `weekly-best-of` → default model
  - `outreach-goodnight` → default model
- **Next session must:**
  1. Re-test `ollama/qwen3.5:4b` directly once, confirm it's now allowed
  2. Audit and remove fallback settings from config level (models config, agents config)
  3. Apply cron model split: first-wave Qwen batch to `ollama/qwen3.5:4b`, rest to MiniMax, Grok/Gemini exceptions stay
  4. Ensure NO fallbacks anywhere — jobs fail at model, don't spill


## Cron / Model Config — UPDATE (2026-04-04 late evening)

- **Config edit applied**: `qwen3.5:4b` was manually added to:
  1. `models.providers.ollama.models[]` — with name "Qwen 3.5 4B", reasoning:true, contextWindow:128000, maxTokens:8192, cost:0
  2. `agents.defaults.models["ollama/qwen3.5:4b"].params` — maxTokens:8192
- Gateway restart is still needed for changes to take effect — `exec` is blocked, could not call `openclaw gateway restart` or `gateway(action="config.patch")` successfully due to hash mismatch issues
- **Pending**: After gateway restart, need to re-test `ollama/qwen3.5:4b` smoke test to confirm model passes runtime allowlist
- **Pending**: After Qwen confirmed working, repoint cron jobs:
  - First wave → `ollama/qwen3.5:4b`: git-maintenance, daily-cost-report, Morning Brief, security:deep-scan, kairos-poll, kairos-morning-digest, kairos-evening-digest, prometheus-learn, prometheus-consolidate, VPStudio bugscan auto-commit
  - Remaining model-bearing jobs → `litellm/MiniMax-M2.7`
  - Grok/Gemini exceptions stay as-is
- `exec` tool is still blocked (allowlist miss) — cannot restart gateway or run shell commands from chat


# 2026-04-04 — late update

## Cron model migration — second batch running

- **Second batch of cron edits now in flight** (MiniMax bulk + qwen model additions + enables), spawned as a subagent. Results pending.
- Previous batch (22 edits via subagent) had silently failed — subagent returned empty output due to rate limits hitting that subagent's own model.
- Verified that MiniMax subagents work fine (tested with verify-jobs subagent — returned 19k tokens output).
- **Direct file writes via read+write tool are the reliable path** — subagents keep getting rate-limited on their own default model and silently failing.
- Qwen smoke test is still `model not allowed` — gateway may need a restart to pick up the newly added qwen3.5:4b config.
- All model-bearing cron jobs still show `openai-codex/gpt-5.4` in verify-read — edits not yet confirmed applied.
- `exec` tool remains blocked in this session — cannot restart gateway or run shell.
- Remaining jobs needing attention (MiniMax, no model, or disabled) are in the second batch.


## Cron model migration — completed

Two subagent batches ran today to migrate cron jobs off `openai-codex/gpt-5.4` (rate-limited) onto local/specialty models:
- **Batch 1 (22 edits)**: silently failed due to subagent rate limits on its own default model
- **Batch 2 (13 edits via subagent `c4608a4a`)**: all succeeded — 13/13

**Final model map applied:**
- `ollama/qwen3.5:4b` (local): git-maintenance, daily-cost-report, security:deep-scan, kairos-poll, kairos-morning-digest, kairos-evening-digest, prometheus-learn, prometheus-consolidate, Morning Brief (enabled)
- `litellm/MiniMax-M2.7`: project-report, cleta-daily-update, dailies-channels, for-you-brief, outreach-morning, outreach-checkin, finance-brief, trend-content-studio, outreach-goodnight, outreach-goodnight-shania, outreach-morning-shania, outreach-checkin-shania, nightshift-keep-working
- `litellm/openrouter-grok-4.1-fast`: twitter-trending (untouched, intentional)
- `litellm/google-gemini-3.1-flash-lite-preview`: dailies-media-pack, dailies-memes-pack, youtube-tech-daily (untouched, intentional)
- Bugscan pipeline (5 jobs on `codex-spark`) + `VPStudio UI Remodel` were not in scope for migration

**Operational notes:**
- `exec` tool was blocked all session — could not restart gateway to pick up newly added qwen model config; Qwen smoke test still showed "model not allowed" at session end
- Direct file reads/writes via tool are the reliable path; subagents fail silently when their own default model hits rate limits
- `cron` tool unavailable in this session context


## Model provider rules (important — do not repeat)

- **`minimax/` prefix = debug/dev only.** Never use `minimax/MiniMax-M2.7` or `minimax/MiniMax-M2.7-highspeed` in production config — these are debug provider endpoints, not the real MiniMax API.
- **`litellm/MiniMax-M2.7` = the correct MiniMax production model.** This routes through the litellm proxy (`https://litellm.tgk30.com/v1`).
- **`litellm/MiniMax-M2.7-highspeed`** is the fast variant via litellm if needed.
- **Default model** should always be `litellm/MiniMax-M2.7` — the user's own key, guaranteed capacity, ~10x cheaper than Codex/GPT.

## Cron config — USER'S FINAL MANUAL EDITS (2026-04-04 ~22:02 ADT)

User manually edited jobs.json directly. Final config summary:

**Every 1hr:**
- `kairos-poll` → `ollama/qwen3.5:4b` (was every 15min, user changed to 1hr — much better)

**Every 3hrs:**
- `prometheus-learn` → `ollama/qwen3.5:4b` (was every 30min, user changed to 3hrs)

**Daily:**
- `git-maintenance` → `litellm/MiniMax-M2.7`
- `project-report` → `litellm/MiniMax-M2.7`
- `cleta-daily-update` → `ollama/qwen3.5:4b` (personal WhatsApp, lightweight)
- `dailies-channels` → `litellm/MiniMax-M2.7`
- `for-you-brief` → `litellm/MiniMax-M2.7`
- `outreach-morning` → `litellm/MiniMax-M2.7`
- `outreach-checkin` → `ollama/qwen3.5:4b` (was MiniMax)
- `twitter-trending` → `litellm/openrouter-grok-4.1-fast` (untouched)
- `finance-brief` → `litellm/MiniMax-M2.7`
- `daily-cost-report` → `ollama/qwen3.5:4b`
- `trend-content-studio` → `litellm/MiniMax-M2.7`
- `weekly-best-of` → `openai-codex/gpt-5.4`
- `dailies-media-pack` → `litellm/google-gemini-3.1-flash-lite-preview` (untouched)
- `dailies-memes-pack` → `litellm/google-gemini-3.1-flash-lite-preview` (untouched)
- `youtube-tech-daily` → `litellm/google-gemini-3.1-flash-lite-preview` (untouched)
- `outreach-goodnight` → `ollama/qwen3.5:4b`
- `outreach-goodnight-shania` → `ollama/qwen3.5:4b`
- `outreach-morning-shania` → `litellm/MiniMax-M2.7`
- `outreach-checkin-shania` → `ollama/qwen3.5:4b`
- `nightshift-keep-working` → `litellm/MiniMax-M2.7` (disabled by user)
- `Morning Brief` → `litellm/MiniMax-M2.7` (was qwen, user changed to MiniMax)
- `security:deep-scan` → `openai-codex/gpt-5.4`
- `kairos-morning-digest` → `litellm/MiniMax-M2.7`
- `kairos-evening-digest` → `litellm/MiniMax-M2.7`
- `prometheus-consolidate` → `ollama/qwen3.5:4b`

**Disabled (all bugscan + UI Remodel):**
- `VPStudio bugscan v2 — manager/collector A/B/C/validator` → DISABLED
- `VPStudio bugscan auto-commit` → DISABLED
- `VPStudio UI Remodel` → DISABLED

**Notable changes from user's manual edit vs. what subagent applied:**
- `Morning Brief`: subagent set qwen, user manually overrode to MiniMax
- `kairos-poll`: user changed from every 15min to every 1hr
- `prometheus-learn`: user changed from every 30min to every 3hrs
- `outreach-checkin`: user changed from MiniMax to qwen
- `cleta-daily-update`: user changed from MiniMax to qwen

**Suggested follow-up:** `security:deep-scan` still on `openai-codex/gpt-5.4` with `thinking: xhigh` — monthly job, low burn frequency but user may want to swap to qwen if the bash script output is text-only and doesn't need Codex reasoning.

# 2026-04-04 — late update (22:11 ADT)

## Full cron jobs inventory (post-user manual edits)

### Every 1hr
| # | Job | Model | Status |
|---|-----|-------|--------|
| 1 | kairos-poll | ollama/qwen3.5:4b | error (timeout 13x) |
| 2 | prometheus-learn | ollama/qwen3.5:4b | ✅ ok, last run 22:04 ADT |

### Every 3hrs
| # | Job | Model | Status |
|---|-----|-------|--------|
| 3 | git-maintenance | litellm/MiniMax-M2.7 | error (rate_limit 3x) |

### Daily
| # | Job | Model | Schedule | Status |
|---|-----|-------|----------|--------|
| 4 | project-report | litellm/MiniMax-M2.7 | 9am AST | error (rate_limit 3x) |
| 5 | cleta-daily-update | litellm/MiniMax-M2.7 | 8pm Dubai | error (fallback exhausted) |
| 6 | dailies-channels | litellm/MiniMax-M2.7 | 8:30am AST | ✅ ok |
| 7 | for-you-brief | litellm/MiniMax-M2.7 | 9am AST | ✅ ok (no delivery) |
| 8 | outreach-morning | litellm/MiniMax-M2.7 | 4am UTC | ✅ ok |
| 9 | outreach-checkin | litellm/MiniMax-M2.7 | 1pm+7pm Dubai | ✅ ok |
| 10 | twitter-trending | litellm/openrouter-grok-4.1-fast | 8:30am AST | ✅ ok |
| 11 | finance-brief | litellm/MiniMax-M2.7 | 8:30am M-F AST | ✅ ok |
| 12 | daily-cost-report | ollama/qwen3.5:4b | 9am AST | ✅ ok |
| 13 | trend-content-studio | litellm/MiniMax-M2.7 | 11am+11pm UTC | ✅ ok |
| 14 | weekly-best-of | openai-codex/gpt-5.4 | Sun 8pm AST | ✅ ok (weekly) |
| 15 | dailies-media-pack | litellm/google-gemini-3.1-flash-lite-preview | 8:35am AST | ✅ ok (no delivery) |
| 16 | dailies-memes-pack | litellm/google-gemini-3.1-flash-lite-preview | 8:40am AST | ✅ ok (no delivery) |
| 17 | youtube-tech-daily | litellm/google-gemini-3.1-flash-lite-preview | 8:32am AST | ✅ ok (no delivery) |
| 18 | outreach-goodnight | litellm/MiniMax-M2.7 | 10:30pm Dubai | error (rate_limit 1x) |
| 19 | outreach-goodnight-shania | litellm/MiniMax-M2.7 | 10:30pm Budapest | error (rate_limit 1x) |
| 20 | outreach-morning-shania | litellm/MiniMax-M2.7 | 8am Budapest | ✅ ok |
| 21 | outreach-checkin-shania | litellm/MiniMax-M2.7 | 1pm+7pm Budapest | error (rate_limit 1x) |
| 22 | nightshift-keep-working | litellm/MiniMax-M2.7 | every 45min | ❌ DISABLED |
| 23 | Morning Brief | litellm/MiniMax-M2.7 | 8am AST | ✅ ok |
| 24 | security:deep-scan | openai-codex/gpt-5.4 | 9am 1st of month | error (unknown channel) |
| 25 | kairos-morning-digest | litellm/MiniMax-M2.7 | 8am AST | ✅ ok |
| 26 | kairos-evening-digest | litellm/MiniMax-M2.7 | 8pm AST | error (rate_limit 1x) |
| 27 | prometheus-consolidate | ollama/qwen3.5:4b | midnight AST | ✅ ok |

### DISABLED
| Job | Model | Reason |
|-----|-------|--------|
| VPStudio bugscan v2 — manager | openai-codex/gpt-5.3-codex | rate_limit |
| VPStudio bugscan v2 — collector A | openai-codex/gpt-5.3-codex-spark | rate_limit |
| VPStudio bugscan v2 — collector B | openai-codex/gpt-5.3-codex-spark | rate_limit |
| VPStudio bugscan v2 — collector C | openai-codex/gpt-5.3-codex-spark | rate_limit |
| VPStudio bugscan v2 — validator | openai-codex/gpt-5.3-codex | rate_limit |
| VPStudio bugscan auto-commit | openai-codex/gpt-5.4 | rate_limit |
| VPStudio UI Remodel — 15m / 3-agent / gpt-5.4 | openai-codex/gpt-5.4 | disabled by user |

## Key issues to watch
- **kairos-poll** (qwen) has timed out 13x in a row — likely the Revolt POST is hanging or the gh commands are slow; job runs every 1hr and keeps failing
- **cleta-daily-update** is hitting `FallbackSummaryError: All models failed (3)` — MiniMax failing, then spillover to Codex also failing (rate limit), then Opus/Sonnet also billing error; needs investigation
- **outreach-goodnight**, **outreach-goodnight-shania**, **outreach-checkin-shania**, **kairos-evening-digest** — all getting `rate_limit` on Codex even though they're configured for MiniMax — suggests the MiniMax provider itself may be failing and falling through to Codex, which is then rate-limited
- **security:deep-scan** is still on `openai-codex/gpt-5.4` with `thinking:xhigh` — monthly job, next run ~May 1st; user may want to migrate to qwen or MiniMax
- **weekly-best-of** is still on `openai-codex/gpt-5.4` — runs weekly on Sundays; user may want to migrate

- **jobs.json checked at 22:17 EDT** — no changes detected since prior read at 22:11. File identical to prior state. User was expecting edits to persist but they didn't save or weren't written. Awaiting clarification from user on what they tried to change.


- **jobs.json CORRECTION at 22:20 EDT** — file has 34 jobs, not 30. My earlier reads were truncated at line limits. Four jobs I missed: `VPStudio bugscan v2 — collector C`, `VPStudio bugscan v2 — validator`, `VPStudio bugscan auto-commit`, `VPStudio UI Remodel`. The UI Remodel job (`370443aa-a1db-413a-8809-8e2078292e93`) is **enabled: false** — was disabled, not actively running. Last run was ok (44882ms). User was frustrated I missed these jobs.

## VPStudio GitHub PR Protocol (2026-04-06)

- **Every VPStudio PR must tag @codex for review.** Standing rule.
- Implemented: `.github/CODEOWNERS` (auto-requests @codex on all PRs) + `.github/pull_requests/pull_request_template.md` (explicit @codex mention in every PR body).
- Git push from this machine hangs (network-level HTTPS timeout). Commit `889fffd6f` (CODEOWNERS/PR template) is local and unpushed. Push from a machine with better connectivity.
