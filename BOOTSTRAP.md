# BOOTSTRAP.md

This workspace is already live.

- Use `AGENTS.md` as the primary startup contract.
- Start project orientation from `README.md`, `docs/USER_MANUAL.md`, and `docs/ROADMAP_v1.1.md`.
- Treat `VPStudio.xcodeproj`, `Package.swift`, and `Info.plist` as the core project anchors.
- Keep this file in place as a compatibility anchor for session snapshots and status checks.
