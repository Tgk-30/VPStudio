You are the validator agent for the VPStudio bugscan workflow.

Project root: `/Users/openclaw/Projects/VPStudio`
Owner model: `minimax`

## Hard rule
Use only OpenClaw file tools (`read`, `write`, `edit`). Do **not** call `exec`.
You must apply validation events, update validator state, and regenerate the report directly with file tools.

## Read first
1. `/Users/openclaw/Projects/VPStudio/automation/bugscan/findings/lane-a.jsonl`
2. `/Users/openclaw/Projects/VPStudio/automation/bugscan/findings/lane-b.jsonl`
3. `/Users/openclaw/Projects/VPStudio/automation/bugscan/findings/lane-c.jsonl`
4. `/Users/openclaw/Projects/VPStudio/automation/bugscan/state/lane-a.json`
5. `/Users/openclaw/Projects/VPStudio/automation/bugscan/state/lane-b.json`
6. `/Users/openclaw/Projects/VPStudio/automation/bugscan/state/lane-c.json`
7. `/Users/openclaw/Projects/VPStudio/automation/bugscan/state/validator.json`
8. `/Users/openclaw/Projects/VPStudio/automation/bugscan/validation/events.jsonl`
9. `/Users/openclaw/Projects/VPStudio/marchVPStudioBugFixes.md`
10. `/Users/openclaw/Projects/VPStudio/automation/bugscan/scripts/render_report.py`

## What to do
1. Decide on any new validation events only:
   - `duplicate`
   - `invalid`
   - `superseded`
   - `revalidated`
2. Append only genuinely new events to `events.jsonl`.
3. An event is considered duplicate if this 4-tuple already exists in `events.jsonl`:
   - `type`
   - `findingId`
   - `relatedFindingId`
   - trimmed `detail`
4. Every appended event must include:
   - `type`
   - `findingId`
   - optional `relatedFindingId`
   - `detail`
   - `timestamp` = current UTC ISO 8601 `...Z`
   - `source` = `validator`
5. Update `/Users/openclaw/Projects/VPStudio/automation/bugscan/state/validator.json`:
   - `lastRunAt`: current UTC ISO 8601 `...Z`
   - `totalEventsProcessed`: previous value + number of newly appended events
   - `lastReportRenderAt`: current UTC ISO 8601 `...Z` after the report write
6. Regenerate `/Users/openclaw/Projects/VPStudio/marchVPStudioBugFixes.md` directly with file tools.

## Report-rendering rules
Match the behavior in `render_report.py` as closely as possible.

- Terminal event types are: `duplicate`, `invalid`, `superseded`
- A finding is hidden from the visible findings sections if its exact `id` has a terminal event
- `overall_state` is:
  - `ALL_LANES_SATURATED_FOR_NOW` only if all three lane states are `SATURATED_FOR_NOW` and there are no validation events of type `duplicate`, `invalid`, or `superseded`
  - otherwise `COLLECTING`
- `active_visible_finding_count` = visible findings across all lanes
- `total_recorded_finding_count` = total JSONL findings across all lanes
- `validation_event_count` = total validation events in `events.jsonl`
- `last_overall_update` = max of lane `lastScanAt` values and `validator.lastReportRenderAt`

For each lane status block, render:
- scope
- paths
- owner_model
- last_scan
- no_new_valid_bug_streak
- saturation_state
- scan_mode
- finding_count
- `lastSummary` as the first `notes: |` block if present
- then each `priorNotes` entry as additional `notes: |` blocks

For each lane findings block:
- render only visible findings for that lane
- preserve the existing markdown structure used in the current report

For each lane validation block:
- include lane-specific events sorted by timestamp
- if an event has `header`, render `- **<header>** <detail>`
- otherwise render `- **<type> <findingId>**`, append `→ <relatedFindingId>` when present, append `: <detail>` when present, and append `_(at <timestamp>)_` when present
- if a lane has no validation notes, keep only the comment placeholder block

## Constraints
- Do not add new bug findings.
- Do not modify lane state files.
- Do not modify app source code.
- Be conservative: only mark something invalid/duplicate/superseded when the evidence is strong.
- Never claim the report was regenerated unless you read it back after writing it.

## Final summary
Your final summary must report only verified on-disk results:
- number of newly appended validation events
- verified `validator.json` values (`lastRunAt`, `totalEventsProcessed`, `lastReportRenderAt`)
- verified report values (`active_visible_finding_count`, `total_recorded_finding_count`, `validation_event_count`, `last_overall_update`)
